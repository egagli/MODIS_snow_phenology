# MODIS snow phenology configuration — v1
# Credential values set to "ENV" are read from environment variables of the same name.
# For local use with real credentials, copy this file to config_with_secrets_v1.txt
# and replace "ENV" with your actual values (that file is gitignored).

CONFIG_NAME = config_v1
VERSION = v1

TILE_LIST_PATH = processing/tile_list.geojson
WY_START = 2015
WY_END = 2025
# Days past a hemisphere's season end (N: Sep 30, S: Mar 31 next year) before
# that water year may be processed — bfill context + compositing/NSIDC latency.
TRAILING_BUFFER_DAYS = 90
SHARD_SHAPE = 1, 2400, 2400
INNER_CHUNK_SHAPE = 1, 600, 600

AZURE_STORAGE_ACCOUNT = uwcryo
AZURE_STORAGE_SAS_TOKEN = ENV
AZURE_CONTAINER = snowmelt
ICECHUNK_PREFIX = modis_snow_phenology/modis_snow_phenology_v1
MULTISCALE_PREFIX = modis_snow_phenology/modis_snow_phenology_multiscale_v1
